Out of the box deployment for a Python Flask application.

To use:
$ vagrant up
$ vagrant ssh

On Vagrant box:
sudo reboot

This will restart the vm which will bring up the flask application on localhost:4567.
